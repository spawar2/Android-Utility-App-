<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gsu.final_project_sp">

    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:name=".view.YanApplication"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CalculatorActivity" />
        <activity android:name=".ViewPagerActivity"></activity>
        <activity android:name=".AnimationActivity" />
        <activity android:name=".AnimatorActivity" />
        <activity android:name=".ListViewActivity" />
        <activity android:name=".DialogActivity" />
        <activity android:name=".Timer_Activity" />


        <service android:name=".audio.MusicService" />

        <activity
            android:name=".view.Music_Play"
            android:label="@string/title_activity_music__play"
            android:theme="@style/Theme.AppCompat.NoActionBar"></activity>

    </application>

</manifest>